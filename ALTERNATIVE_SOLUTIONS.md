# WEBMAX - حلول بديلة للتشغيل على Windows

## الحل 1: WSL2 (الأسهل والمجاني) ⚡

WSL = Windows Subsystem for Linux (مُثبت مسبقاً على Windows 10/11)

### الخطوات:
```powershell
# 1. تفعيل WSL (Run as Administrator)
wsl --install

# 2. إعادة تشغيل الكمبيوتر

# 3. بعد الإعادة، افتح Ubuntu واكتب:
sudo apt update && sudo apt upgrade -y

# 4. تثبيت المتطلبات
sudo apt install python3-dev python3-pip python3-venv redis-server mariadb-server npm git -y

# 5. تثبيت Bench
pip3 install frappe-bench

# 6. إنشاء بيئة WEBMAX
bench init webmax-bench

# 7. انتقل للمجلد
cd webmax-bench

# 8. إنشاء موقع
bench new-site webmax.local

# 9. تثبيت التطبيق
bench get-app https://github.com/webmax/webmax
bench --site webmax.local install-app webmax

# 10. تشغيل
bench start
```

**المميزات:**
- ✅ مجاني
- ✅ يعمل مباشرة على Windows
- ✅ أداء ممتاز
- ✅ يدعم جميع مميزات ERPNext

---

## الحل 2: GitHub Codespaces (سحابي + مجاني) ☁️

### الخطوات:
1. ارفع المشروع على GitHub
2. افتح المشروع على GitHub
3. اضغط على زر `.` (النقطة) في الكيبورد
4. أو اضغط Code → Codespaces → Create codespace

**المميزات:**
- ✅ مجاني (40 ساعة/شهر)
- ✅ لا يحتاج تثبيت
- ✅ يعمل من المتصفح
- ✅ بيئة Linux كاملة

---

## الحل 3: Frappe Cloud (الأسهل للإنتاج) 🚀

### الخطوات:
1. سجل على: https://frappecloud.com
2. أنشئ حساب جديد
3. اضغط "New Site"
4. اختر "ERPNext" (سنعدلها لـ WEBMAX)
5. بعد الإنشاء، ارفع تعديلاتك

**المميزات:**
- ✅ لا يحتاج إعداد
- ✅ نشر فوري
- ✅ دومين جاهز
- ✅ صيانة تلقائية

**السعر:**
- خطة مجانية متاحة
- أو ~$10/شهر للمواقع الصغيرة

---

## الحل 4: VPS رخيص (للإنتاج الكامل) 💪

### مزودين موصى بهم:
1. **DigitalOcean** - $6/شهر
2. **Hetzner** - €4.5/شهر (أرخص)
3. **AWS Lightsail** - $5/شهر

### الخطوات:
```bash
# على VPS Ubuntu 22.04

# 1. تحديث النظام
apt update && apt upgrade -y

# 2. تثبيت Docker
curl -fsSL https://get.docker.com | sh

# 3. تشغيل WEBMAX
git clone https://github.com/webmax/webmax
cd webmax
docker compose up -d
```

**المميزات:**
- ✅ تحكم كامل
- ✅ دومين مخصص (webmax-erp.site)
- ✅ أداء عالي
- ✅ نسخ احتياطي يومي

---

## 🎯 توصيتي لك:

### للتجربة السريعة:
**استخدم WSL2** → مجاني وسريع

### للنشر الفعلي:
**استخدم Frappe Cloud** → سهل وسريع

### للتحكم الكامل:
**استخدم Hetzner VPS** → رخيص وقوي

---

## 🛠️ للبدء الآن مع WSL2:

افتح PowerShell كـ **Administrator** واكتب:

```powershell
wsl --install
```

ثم:
```powershell
wsl
```

ستدخل على Ubuntu، أكمل التثبيت:

```bash
curl -sL https://raw.githubusercontent.com/frappe/bench/develop/install.py | python3
```

ثم:
```bash
bench init webmax
```

---

هل تريد أن أساعدك في تنفيذ أي من هذه الحلول خطوة بخطوة؟
